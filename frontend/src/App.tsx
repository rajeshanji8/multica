export function App() {
  return (
    <main className="app">
      <h1>Todo App</h1>
      <p>
        Frontend scaffolding is in place. The real UI — add, toggle, edit, delete and filter todos —
        lands in <strong>T3 (PUN-9)</strong>, talking to the API from <strong>T2 (PUN-8)</strong>.
      </p>
    </main>
  );
}
